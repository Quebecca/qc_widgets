mod{
    qcWidgets{
        //This settings is to allow the user to select the number of records to show in the widgets
        lastCreatedPages{
            limit = 0
        }
        lastModifiedPages{
            limit = 0
        }
        workspaceProviderLinks{
            limit = 0
        }
        pagesWithoutModification{
            limit = 0
            numberOfMonths = 3
        }
        recentlyModifiedContent{
            limit = 0
        }
        listOfmembers{
            dontLookintoSubgroups = 1
        }
        numberOfRecordsByType{
            // test si la table ne contient pas les deux clononnes hidden et disabled
            fromTable =  pages, tt_content, be_groups, be_users,sys_category, tx_gabarit_pgu_form_comments_problems, tx_pgumediateurscivils_mjq_mediateurscivils
            totalRecords = 1
            totalNewLast24h = 1
            excludeDisabledItems = 0
            excludeHiddenItems = 0
            numberOfDays = 365
        }
    }
}


